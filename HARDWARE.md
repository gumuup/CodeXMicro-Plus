# 5.0 硬件设置

设置工具栏中的 **硬件** 位于「数据」和「关于」之间。

## 设备

- Chromecast Voice Remote：VID `18D1` / PID `9450`。
- X6 Remote：VID `1D5A` / PID `C081`。
- 设置按型号保存，默认关闭接管。先在 macOS 蓝牙中配对，然后启用映射；如提示权限问题，按页面入口检查输入监控和辅助功能。其他遥控器应用独占设备时，需要先退出该应用。
- X6 鼠标模式键由固件处理，不能单独映射；其鼠标移动和键盘背面保留透传。

## 自由映射

1. 点击某个按键右侧的功能按钮，或启用映射后点「监听遥控器」并按实体键定位。
2. 选择操作类型。支持项目已有的快捷键、应用、系统应用、插件、网址、文本、文件夹、快捷指令和 Codex 工具箱动作。
3. 选择「快捷键」后点击录制按钮，实际按下目标键。硬件编辑支持单键、Esc、左右修饰键、Fn，以及组合键；再次点击录制按钮可取消。
4. 分别配置「按下时」「松开时」。长按语音快捷键可勾选「按住目标键，松开遥控器时释放」；开始和结束使用不同快捷指令的软件可分别配置两个动作。
5. 保存后生效。「未设置」表示禁用动作；「恢复此键默认功能」移除该键自定义配置。

监听只定位按键，不执行其动作。编辑期间暂停项目已有快捷键触发，避免录制时误触。断连、关闭映射、编辑或更换绑定时释放已按住的目标键。映射包含在原有数据备份、恢复及完整性检查中。

## 语音软件与音频来源

语音键不绑定特定语音软件。请使用目标软件实际配置的全局快捷键或 macOS 快捷指令：例如点按切换的语音软件，只配置按下动作；需要持续按住的语音软件，录入其快捷键并勾选按住目标键。

- **Mac 麦克风**：不勾选「使用遥控器麦克风」，由目标语音软件采集其当前输入设备。
- **遥控器麦克风**：勾选该选项，需要已安装 `vRemoteDr 2ch` 或 `BlackHole 2ch`。在目标语音软件中将输入设备设为页面显示的同名设备。CodeXMicro 将 ATVV 音频解码后发送到虚拟设备，不保存录音文件。
- 不附带或改写系统音频驱动，不自动更改系统默认麦克风。若没有虚拟设备，页面会提示依赖缺失。第三方语音软件自身的权限与快捷键配置仍须就绪。
- 遥控器音频跟随设备 ATVV 会话；最长 120 秒自动关闭以避免遗留开麦。不同固件的短按/长按、第三方软件热键响应和音频链路仍需实体设备验证。

## 开发与验证

源码来源及许可见 `ThirdParty/vRemoter/README.md`。完整上游源码保存在 `.build/vendor/vRemoter`。

```sh
swift test
./script/test_native.sh
./script/build_and_run.sh --verify
```

已验证：7 项新增测试通过（型号隔离、任意语音动作、断连/编辑释放、监听防误触、两款 HID 报文、配置序列化、ATVV 解码重置），原有原生测试通过，5.0 应用构建与启动通过。UI 已看到「硬件」页以及本机 X6 连接状态。尚未进行实体按键逐键操作、Chromecast 真机和实际收音验证。

## 硬件页布局与权限入口

- 左侧按型号选择设备，中间展示对应遥控器，右侧滚动配置按键；底部提供语音选项。
- 保留监听定位、自由动作编辑和按下／松开动作；「恢复默认」确认后仅重置当前型号映射。
- 辅助功能、输入监控、麦克风、蓝牙统一在「通用 → 系统权限」管理，直接读取 macOS 状态并定期刷新。已授权时只显示状态；受限权限显示系统限制。
- 遥控器图片复用 vRemoter 的 `Resources/RemoteImages`，构建时复制到应用资源目录，遵循 `ThirdParty/vRemoter/LICENSE`。
- 已实机核对两款遥控器布局及通用页四项已授权状态；本次未更改系统权限。

## 10 组硬件预设

硬件页在按键区域下方提供与轮盘共用的 `PresetCombinationPicker`（1–10）。
每个型号独立保存当前预设编号与按键动作；快捷键、按住释放和语音键的按下／松开动作都包含在预设内。启用映射与遥控器麦克风开关仍为设备级设置。

旧版配置缺少 `presetBanks` 时按第 1 组处理，原映射不会丢失；第 2–10 组初始为设备默认功能。每次修改自动保存；重启后恢复所选组。恢复默认仅影响当前组。
切换预设会取消学习、释放已按住的输出键，并通知硬件服务更新配置。配置沿用 `hardware.remotes.v1`，纳入现有备份。

新增回归验证：旧配置迁移、型号隔离、重启恢复、只重置当前组、无效编号保护，以及切组释放按键并结束学习。

## X6 连接恢复（2026-09-15）

本机 X6 VID/PID 正确，但独占 IOHIDManagerOpen 返回 0xe00002c1（kIOReturnNotPrivileged）。此前直接退出且没有恢复，导致界面显示未连接、自定义按键没有输入来源。
现增加非独占兼容监听、启动快照及断线/打开失败重试，并将按键通道状态从蓝牙语音状态中独立显示。兼容模式保留未配置的原生按键，自定义标准键盘及音量/静音媒体键通过短暂事件关联过滤原生事件。
已实机确认连接恢复及 X6 电量 69%；23 项回归通过。实体快捷键的最终触发效果待用户复测确认。

## 5.1.0 可靠性更新（2026-09-15）

- X6 语音键专用于切换遥控器麦克风：按一下持续开启、再按一下关闭，松开按键不结束收音，也不执行自定义松开动作。
- 部分 X6 固件会在首次按键松开后结束语音流；应用会进行有限次数的会话恢复，开麦错误也只做有界重试，避免持续命令循环。
- 电池通道每 30 秒主动刷新。暂时读取不到或收到无效百分比时保留最近一次有效读数；设备断开时界面仍显示未连接。
- 快捷键、应用、系统应用和网址映射直接执行，减少等待 Codex 界面刷新造成的硬件响应延迟。
- X6 HID 边沿先到时立即过滤对应的系统原生事件，只有系统事件先到时才等待 25 ms 识别来源。
