# 5.0 音频设置

设置工具栏中的 **音频** 位于「通用」之后。

## 顶部音频控制台

- 卡片跟随 macOS 浅色／深色外观，统一间距与控件样式。每页显示两路输入，混合输出固定在右侧；更多输入可翻页查看。
- 显示实际输入电平、混合输出电平，支持快速切换系统输入、参与混音、静音、独奏和输出设备。
- 下方输入列表中的「隐藏卡片」立即隐藏对应顶部卡片，取消勾选即可恢复；不会改变设备的混音选择、静音或系统默认输入。
- 隐藏偏好按设备 UID 保存到 `audio.hiddenCards.v1`，重启后保留，并纳入数据备份。

## 系统声音联动

- 「音频输入」「音频输出」直接读取和设置 macOS 默认设备，系统设置中的变化也会同步显示。
- 内置、USB、蓝牙及虚拟音频设备通过 CoreAudio 自动发现，不限厂牌。DJI MIC MINI2 等设备只要被 macOS 识别为音频输入，就会出现在列表中。
- 输入／输出音量和静音使用设备提供的系统控制。无系统音量控制的设备显示说明，不伪造可用滑块。
- 「测试麦克风」只显示当前输入的电平，不播放或保存录音；再次点击停止，离开音频页也会停止测试。
- 「机械按键声音」与通用页共用设置，关闭触感不会连带关闭声音。声音效果输出与 macOS 对应选项联动。
- 已在语音软件内单独指定设备时，该软件可能不跟随系统默认输入；需在软件内修改。

## 多设备混合输入与输出

1. 勾选参与混合的输入，例如 MacBook 麦克风、USB 麦克风或遥控器麦克风。
2. 勾选一个或多个输出。所有所选输入先混合，再同时送入每个输出。
3. 点击「启动混音」。首次采集系统麦克风时，macOS 会请求麦克风权限。修改输入或输出会停止当前混音，确认选择后需再次手动启动，不会意外自动收音。
4. 每路输入支持增益、静音、独奏；每路输出支持独立音量和静音。控制实时生效，电平随混音更新。
5. 点击「停止混音」释放采集和输出。增益、选择和静音配置会保存；重启应用不会自动收音。

「内置麦克风 + 已连接遥控器」可快速准备配置。遥控器的直接音源使用 `remote:x6` / `remote:chromecast`，需先在硬件页启用对应映射和遥控器麦克风。运行混音时，遥控器音频统一进入混音器，不再重复走原先的独立输出。

### 供语音软件使用

建议选择：内置麦克风 + 遥控器直接音源 → `vRemoteDr 2ch` 或 `BlackHole 2ch`。然后将系统或语音软件的输入设备设为该虚拟音频设备。

同一虚拟音频设备不能同时作为混音输入和输出，否则会形成回路。例如将 vRemoteDr 作为输出时，不要同时把它当作输入，应选择遥控器的直接音源。真实双向 USB 音频接口仍可同时收音和输出。

### 多设备输出与边界

- 混音可以同时送往耳机、扬声器、显示器或虚拟音频设备。输入重采样到 48 kHz 后混合，输出引擎适配设备的采样率。
- 输出到扬声器会播放现场麦克风声音；扬声器靠近麦克风可能造成回声或啸叫。
- 不同物理设备有独立时钟；蓝牙与有线设备可能有不同延迟。本版提供有界缓冲，不保证多个硬件输出逐采样同步。
- 这里混合的是所选输入设备，其他应用的播放声音需先通过虚拟音频设备接入；不自动抓取所有应用声音，也不自动创建系统聚合设备。
- 设备格式变化导致输出暂停时尝试有限次数恢复；设备断开或无法恢复时停止混音并提示。
- 保存配置中的设备暂时断开时，本次启动会跳过不可用项并保留原配置；仍有可用输入和输出即可继续混音，设备恢复后无需重新选择。
- 配置键为 `audio.mix.v1`，纳入现有的数据备份、恢复和完整性检查。

## 验证

- `CODEXMICRO_AUDIO_IO_SMOKE=1 swift test`：16 项测试通过，覆盖真实求和、独奏/静音、每路输出增益、限幅、FIFO 溢出、分包重采样、分贝显示、路由回路检查、配置持久化、输出图启动及原有遥控器逻辑。
- `CODEXMICRO_AUDIO_IO_SMOKE=1 swift test --filter virtualOutputGraphOpensWithoutCapturingOrPlayingContent`：本机 vRemoteDr 2ch 输出图启动成功；测试只送静音，不采集麦克风。
- `./script/test_native.sh`：原有原生回归通过。
- `./script/build_and_run.sh --verify`：5.0 构建和启动通过。
- 2026-09-15 实机验证：MacBook Air 麦克风与 DJI Mic Mini 2-E7DCC2（蓝牙）同时采集到真实电平，输出至 vRemoteDr 2ch；DJI 独奏正常，输出静音后输出电平归零而输入继续更新。测试后已停止采集。
- UI 已验证勾选隐藏后 DJI 顶部卡片消失，混音选择保持；取消隐藏恢复。浅色布局已实机核对，深色使用系统语义颜色；蓝牙延迟和第三方语音软件识别效果未测量。
